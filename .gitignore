# Local environment files — contain secrets, never commit
.env
.env.dev
.env.test

# Production sync credentials
scripts/.env.prod-sync

# Docker override for local customizations
docker-compose.override.yml
